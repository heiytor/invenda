# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

api/app

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work
go.work.sum

# secrets (e.g.) ed25519 key pais
secrets/*
